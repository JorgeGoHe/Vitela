{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-print",
    {
      "identifier": "opener:allow-open-url",
      "allow": [{ "url": "http://*" }, { "url": "https://*" }, { "url": "mailto:*" }]
    },
    {
      "comment": "abrir un adjunto con el visor del sistema: open_attachment lo deja en una carpeta vitela-adjunto-… del temporal y devuelve esa ruta, así que el permiso se acota al temporal y no al disco entero",
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "$TEMP/**" }]
    },
    "dialog:default"
  ]
}
